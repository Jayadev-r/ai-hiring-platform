{
    "name": "ai-hiring-platform-root",
    "version": "1.0.0",
    "description": "Root project for AI Powered Agentic Hiring Platform",
    "scripts": {
        "start": "concurrently --kill-others-on-fail --prefix-colors \"blue,cyan\" --names \"BACKEND,FRONTEND\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
        "dev": "npm start",
        "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
        "build": "npm run build --prefix frontend"
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "nodemon": "^3.0.2"
    }
}